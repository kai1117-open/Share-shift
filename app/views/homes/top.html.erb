<div class="container mt-5">
  <!-- カード全体 -->
  <div class="card mx-auto shadow-lg" style="max-width: 800px; border: none;">
    <!-- 画像を上部に収める -->
    <img src="<%= asset_path 'hero-background.jpg' %>" alt="Hero Image" class="card-img-top" style="object-fit: cover; height: 300px;">
    
    <!-- コンテンツ部分 -->
    <div class="card-body text-center bg-dark text-white rounded-bottom">
      <h1 class="display-5 fw-bold mb-3 text-uppercase">ようこそ</br>Share-shiftへ</h1>
      <p class="lead mb-4">シフトの共有で、仕事がもっと便利に。</p>
      <%= link_to '新規会員登録', new_user_registration_path, class: 'btn btn-outline-light btn-lg mx-2' %>
      <%= link_to 'ログイン', new_user_session_path, class: 'btn btn-light btn-lg mx-2', method: :post %>
    </div>
  </div>
</div>

<style>
  /* カードのカスタマイズ */
  .card {
    overflow: hidden; /* 画像がカード外に出ないように */
  }

  .card-img-top {
    width: 100%;
  }

  .bg-dark {
    background-color: rgba(0, 0, 0, 0.7); /* カード下部背景 */
  }

  .btn {
    font-size: 1.2rem;
  }
</style>
